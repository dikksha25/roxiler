# ==========================================================
# Store Rating Frontend — Environment Configuration
# ==========================================================

# Production API Endpoint (Leave blank or set to '/api/v1' for same-origin proxy)
VITE_API_URL=http://localhost:5000/api/v1
